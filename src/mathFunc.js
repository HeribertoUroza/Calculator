
const addFunc = (displayValue, previousValue) => {
    // let sum = previousValue;
    // sum += displayValue
    console.log('hehehehhe')
    return displayValue + previousValue
}

export {
    addFunc
}